table! {
    users (id) {
        id -> Int4,
        first_name -> Nullable<Varchar>,
        last_name -> Nullable<Varchar>,
        age -> Nullable<Int4>,
        hobby -> Nullable<Varchar>,
        email -> Nullable<Varchar>,
    }
}
